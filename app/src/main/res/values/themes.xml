<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema principal de la app -->
    <style name="Base.Theme.Aye_Aye" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Puedes personalizar más colores aquí -->
        <!-- <item name="colorPrimary">@color/primary_color</item> -->
    </style>

    <style name="Theme.RibbitDaryPersonal" parent="Base.Theme.Aye_Aye" />

    <!-- Tema del Splash Screen para Android 12+ -->
    <style name="Theme.RibbitDaryPersonal.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/azul</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo1</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Aye_Aye</item>
    </style>
</resources>
